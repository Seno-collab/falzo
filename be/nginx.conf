events{}

http {
    upstream api_backend {
        server app:8000;
    }
    server {
        listen 80;
        # Server frontend
        location / {
            proxy_pass http://web:3000;
        }
        # API route
        location /api/ {
            proxy_pass http://api_backend/;
        }
    }
}
